Git - система управления версий, позволяющая разработчикам отслеживать изменения в проекте

`git --version` - команда для проверки установленной версии гит 

`git init` - создает новый репозиторий в указанном каталоге